apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
    - deployment.yaml
    - ingress.yaml
    - service.yaml
    - secret.enc.yaml

configMapGenerator:
    - name: grafana-datasources
      files:
          - datasources.yaml
    - name: grafana-alerting
      files:
          - alerting.yaml
