apiVersion: apps/v1
kind: Deployment

metadata:
    name: actual

spec:
    replicas: 1
    strategy:
        type: Recreate
    selector:
        matchLabels:
            app: actual
    template:
        metadata:
            labels:
                app: actual
        spec:
            enableServiceLinks: false
            containers:
                - name: actual
                  image: ghcr.io/actualbudget/actual:latest-alpine
                  env:
                      - name: ACTUAL_PORT
                        value: "5006"
                  ports:
                      - name: http
                        containerPort: 5006
                  resources:
                      requests:
                          cpu: 50m
                          memory: 128Mi
                      limits:
                          memory: 512Mi
                  volumeMounts:
                      - name: data
                        mountPath: /data
            volumes:
                - name: data
                  hostPath:
                      path: /var/data/local/actual
                      type: Directory
