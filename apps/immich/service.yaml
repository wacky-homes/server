apiVersion: v1
kind: Service

metadata:
    name: immich
    namespace: default

spec:
    selector:
        app: immich
    ports:
        - name: http
          port: 2283
          targetPort: http
    type: ClusterIP
