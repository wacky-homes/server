apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
    name: vaultwarden
    namespace: default
    annotations:
        traefik.ingress.kubernetes.io/router.entrypoints: web

spec:
    ingressClassName: traefik
    rules:
        - host: vaultwarden.wacky.homes
          http:
              paths:
                  - path: /
                    pathType: Prefix
                    backend:
                        service:
                            name: vaultwarden
                            port:
                                name: http
        - host: vaultwarden.wacky.local
          http:
              paths:
                  - path: /
                    pathType: Prefix
                    backend:
                        service:
                            name: vaultwarden
                            port:
                                name: http
