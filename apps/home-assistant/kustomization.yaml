apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
    - deployment.yaml
    - ingress.yaml
    - service.yaml
    - networkpolicy.yaml

configMapGenerator:
    - name: home-assistant-config
      files:
          - configuration.yaml
