apiVersion: v1
kind: Service

metadata:
    name: whoami
    namespace: default

spec:
    selector:
        app: whoami
    ports:
        - name: http
          port: 80
          targetPort: http
    type: ClusterIP
